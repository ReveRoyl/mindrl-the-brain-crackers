This folder can contain local small artifacts needed for the submission.

Do not commit:

- large checkpoints
- private keys
- API tokens
- raw private data

Large model artifacts should be referenced through Hugging Face checkpoint paths in `submission.yaml` or `config.yaml`.
